# SQLite database used by Prisma
DATABASE_URL="file:./dev.db"

# App configuration consumed by ConfigModule.forRoot()
PORT=3000
# Payment gateway used by PaymentsModule.forRoot({ gateway }): "fake" | "stripe"
PAYMENT_GATEWAY=fake
# Toggle the express-style request logging interceptor
ENABLE_REQUEST_LOGGING=true
